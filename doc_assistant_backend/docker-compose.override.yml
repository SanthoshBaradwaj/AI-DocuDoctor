version: "3.9"
services:
  api:
    volumes:
      - ./app:/app/app
    command: >
      sh -c "python -m app.db_init &&
             uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload"
  worker:
    volumes:
      - ./app:/app/app
